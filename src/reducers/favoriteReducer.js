const initialState = {
  arrFavorites: [],
  boolDisplay: true,
}

const favoriteReducer = (state = initialState, action) => {
  switch(action.type) {
      default:
          return state;
  }
}

export default favoriteReducer;